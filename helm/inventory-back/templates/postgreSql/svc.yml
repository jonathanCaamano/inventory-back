apiVersion: v1
kind: Service
metadata:
  labels:
    app: postgresql-inventory
  name: postgresql-inventory
  namespace: taller-inventario
spec:
  ports:
  - port: 5432
    protocol: TCP
    targetPort: 5432
  selector:
    app: postgresql-inventory