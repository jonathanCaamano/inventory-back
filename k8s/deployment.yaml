apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-back
  namespace: inventory
  labels:
    app: inventory-back
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventory-back
  template:
    metadata:
      labels:
        app: inventory-back
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: inventory-back
          # Image is updated by CI/CD via `kubectl set image`
          image: ghcr.io/jonathancaamano/inventory-back:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: inventory-back-config
            - secretRef:
                name: inventory-back-secrets
          # /livez always returns 200 — safe for liveness (no external deps)
          livenessProbe:
            httpGet:
              path: /livez
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
            failureThreshold: 3
          # /health checks DB + MinIO — used for readiness
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-back-service
  namespace: inventory
spec:
  selector:
    app: inventory-back
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: inventory-back-ingress
  namespace: inventory
spec:
  ingressClassName: nginx
  rules:
    - host: invent-back.jcrlabs.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: inventory-back-service
                port:
                  number: 8080
